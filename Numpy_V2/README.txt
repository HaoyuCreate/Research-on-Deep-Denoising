1. This is a demo code about the proposed denoising algorithm

2. To run the algorithm, make sure pip  (a defaul tool when install python) has been installed and then conduct two commands:

$ pip install -r requirements.txt

$ python Demo_program.py

3. Output images are record in './Figs' folder. 